@page
@model MusicSchoolWEB.Pages.MembruInstrumente.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MembruInstrument[0].Membru)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MembruInstrument[0].Intrument)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MembruInstrument) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Membru.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Intrument.Nume)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
